const data = require('./data.json');

function* generate(data) {
  // add your code
}

for (let singleLoop of generate(data)) {
  console.log(singleLoop);
  // 첫번째 변호사 이름
  // 첫번째 변호사 사무실 번호
  // 첫번째 변호사 사무실 이름
  // 두번째 변호사 이름
  // ...
}
